---
version: '3.3'

services:
  whoami:
    image: emilevauge/whoami
    networks:
      - traefik-net
    deploy:
      labels:
        traefik.port: 80
        traefik.frontend.rule: "Host:whoami1.mytraefik.de"

networks:
  traefik-net:
    external: true
